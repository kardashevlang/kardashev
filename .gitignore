# Rust build artifacts (Cargo.lock IS committed — this is a toolchain binary).
/target/

# Native artifacts the C backend may leave behind locally.
*.o
*.a
*.so
*.dylib

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*.swo

# Claude Code worktrees / local agent state
.claude/
