// Import fixture (§22.1): deliberately UNPARSEABLE — the importing test pins
// that a sub-file parse error is wrapped into one E0294 (fixtures are skipped
// by the spec runner, so this file is never compiled standalone).
fn oops( {
